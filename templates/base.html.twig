<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}

            <link rel="stylesheet" href="https://bootswatch.com/5/slate/bootstrap.min.css">
        {% endblock %}

        {% block javascripts %}

        {% endblock %}
    </head>
    <body>
            <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
                <div class="container-fluid">
                    <a class="navbar-brand" href="{{ path('film') }}">Films</a>
                </div>


                {% if app.user %}
                    <a href="{{ path('signout') }}" class="btn btn-danger">Deconnect</a>
                    <a href="{{ path('new_film') }}" class="btn btn-info">Create</a>
                    {% else %}
                        <a href="{{ path('app_register') }}" class="btn btn-light">Register</a>
                        <a href="{{ path('signin') }}" class="btn btn-success">Connect</a>
                {% endif %}


            </nav>
        {% block body %}{% endblock %}
    </body>
</html>
